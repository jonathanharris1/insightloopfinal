<%= simple_form_for [chat, message], html: {class: "ai-widget__composer"} do |f| %>
    <%= f.input :content,
                as: :string,
                label: false,
                required: true,
                wrapper: false,
                input_html: {
                  class: "ai-widget__input",
                  autocomplete: "off",
                  placeholder: "Type your question…",
                  data: {ai_chat_target:"input"}
                } %>
      <button class="ai-widget__send" type="submit" aria-label="Send">
        <i class="fa-solid fa-arrow-up"></i>
      </button>
<% end %>
